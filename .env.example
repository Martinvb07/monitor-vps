PORT=3000
JWT_SECRET=cambia_esto_por_un_string_largo_aleatorio_min32chars
ADMIN_USER=martin
# Generar con: node -e "require('bcrypt').hash('tupassword',10).then(console.log)"
ADMIN_PASSWORD_HASH=
ALLOWED_ORIGIN=https://monitor.tudominio.com
NGINX_LOG=/var/log/nginx/access.log
